export const FETCHED_STOCK_DETAILS = (state, data) => {
    state.stockDetails = data;
}

export const FETCHED_COMPANY_INFO = (state, data) => {
    state.companyInfo = data;
}

export const FETCHED_CHART_DETAILS = (state, data) => {
    state.chartDetails = data;
}

export const FETCHED_CRYPTO_DATA = (state, data) => {
    state.cryptoData = data;
}